package com.blockforge.chaoscraft.modes.freezingice.attacks;

import com.blockforge.chaoscraft.ChaosCraftPlugin;
import com.blockforge.chaoscraft.modes.calamity.attacks.AttackRegistry;

/**
 * FreezingIce — MODELENGINE ATTACKS (placeholder).
 *
 * ModelEngine attacks pending — user will provide bbmodel specs in a later phase.
 * Kept as a stub so FreezingIceMode.registerAllAttacks() compiles.
 */
public final class FreezingIceModelEngine {
    private FreezingIceModelEngine() {}

    public static void registerAll(ChaosCraftPlugin plugin, AttackRegistry registry) {
        // ModelEngine attacks pending — user will provide specs
    }
}
